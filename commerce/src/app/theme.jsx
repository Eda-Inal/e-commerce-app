// theme.js
import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  styles: {
    global: {
   
      body: {
        bg: '#FFEFF4', 
        color: 'black',
      },
    },
  },
});

export default theme;
