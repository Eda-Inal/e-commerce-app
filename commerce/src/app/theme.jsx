// theme.js
import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  styles: {
    global: {
   
      body: {
        bg: '#fffff', 
        color: '#3A3A3A',
      },
    },
  },
});

export default theme;
